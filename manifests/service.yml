apiVersion: v1
kind: Service
metadata:
    name: srpazuredevopskubernetesterraformpipeline
spec:
    type: LoadBalancer
    ports:
    - port: 5000 
    selector:
        app: srpazuredevopskubernetesterraformpipeline